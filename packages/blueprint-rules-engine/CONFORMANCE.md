# Rules conformance report

> Generated by `packages/blueprint-rules-engine/tools/conformance-report.js`. Do not edit by hand — re-run it.

**Run:** 2026-09-24  
**Corpus:** `packages/blueprint-rules-engine/tests/conformance/cases.json` — 20 cases over 4 graphs

## What this is

The conformance corpus states what *any* engine evaluating a Blueprint graph must produce. It names no particular engine. This report is the result of running engines against it.

The blueprint does not mandate an evaluation engine — the reference implementation is replaceable by any engine that satisfies the contract. That claim is only meaningful if there is something concrete to satisfy, and something that reports whether an engine does. This is that report.

**The design of conformance as a contract artifact is still a work in progress.** The corpus currently lives as JSON inside the reference implementation's tests rather than as a first-class contract artifact with a schema, a naming convention, and validation alongside the other contract types. Treat its location and format as provisional; the behaviours it pins are not.

## Method

Each case names a graph, the inputs to evaluate it against, and the nodes an engine must produce. Each engine is given the same graph and inputs, and its output is compared against the same expectations, using the corpus's own matching rules — only the fields a case lists are asserted, `missing` is compared as a set, and `messageIncludes` is a substring check.

| outcome | meaning |
|---|---|
| **agrees** | produced the nodes the corpus requires |
| **differs** | produced something else |
| **cannot express** | the graph does not translate to that engine, so the case cannot be run |

A `differs` result is a question, not a verdict. It may mean the engine is wrong, or that the corpus has encoded something it should not.

## Summary

| engine | agrees | differs | cannot express | total |
|---|---|---|---|---|
| @codeforamerica/blueprint-rules-engine | 20 | 0 | 0 | 20 |
| IRS Direct File FactGraph | 10 | 5 | 5 | 20 |

## @codeforamerica/blueprint-rules-engine

**Version:** 0.1.0

The reference implementation in this repository: a CEL evaluator over the compiled graph, running in Node and the browser.

| case | graph | outcome | detail |
|---|---|---|---|
| every input supplied — complete, and outputs are distinguished from intermediates | eligibility | agrees | — |
| a declared default is used — placeholder, and it travels to dependents | eligibility | agrees | — |
| defaults apply per field, not per namespace | eligibility | agrees | — |
| an unreachable input — missing, naming the path | eligibility | agrees | — |
| missing accumulates the paths from every unresolved dependency | eligibility | agrees | — |
| an absent collection is missing, and names itself | eligibility | agrees | — |
| a null collection is an answer: empty, and placeholder | eligibility | agrees | — |
| an empty array is a complete answer | eligibility | agrees | — |
| a wrong-type input errors its dependents and leaves the rest intact | eligibility | agrees | — |
| an integer input is an integer to the expression | numeric | agrees | — |
| division between integers truncates | numeric | agrees | — |
| a number input keeps fractional semantics | numeric | agrees | — |
| a wrong-type sub-field errors every fact reading the collection | collection | agrees | — |
| collection sub-fields read normally when well-typed | collection | agrees | — |
| an absent collection blocks every fact that reads it | collection | agrees | — |
| modulo | operators | agrees | — |
| conditional expression returns either branch | operators | agrees | — |
| conditional expression takes the false branch | operators | agrees | — |
| membership against a list literal | operators | agrees | — |
| membership against a list literal — absent | operators | agrees | — |

Every case in the corpus passes.

## IRS Direct File FactGraph

**Version:** built from [IRS-Public/direct-file@`e0d5c8445`](https://github.com/IRS-Public/direct-file/tree/e0d5c8445/direct-file/fact-graph-scala), SHA-256 `8033fc086c45f0d7748dfe7a6fe0c3a6f6864ee3227c2640c806b59eabdfc621`

A Scala engine compiled to JavaScript, and the only widely known prior art for partial evaluation over a dependency graph. Included because an independent implementation is the only way to tell whether the contract is sound or merely self-consistent. It is a comparison, not an authority.

The bundle is a Scala.js build artifact rather than an upstream file — Direct File consumes it as the workspace package `@irs/js-factgraph-scala@0.0.1`, which is not published to npm, so the commit above names the source it is built from, not a downloadable file. That source has a single commit: `IRS-Public/direct-file` was published as one drop on 2025-05-29 and `fact-graph-scala` has not changed since, so there is exactly one public version of it. Our copy is vendored at `packages/blueprint-rules-engine/vendor/fg.js` and pinned by SHA-256.

### Notes on this comparison

- **A wrong-type input throws rather than reporting.** The type-error case below reads as *agrees*, but only because the bridge in `tools/fact-graph.js` type-checks inputs before seeding and marks the dependent facts itself. Seeded directly, FactGraph raises `ClassCastException` and the whole evaluation is lost, including the facts that would have resolved. Reporting it as a node instead is a deliberate difference, and the reason the bridge exists.
- **Graphs are translated, not authored.** FactGraph reads its own XML dictionary, so each graph is converted by `toFactGraph`. A `cannot express` result means the translation has no equivalent for a construct, which is a limit of FactGraph's node vocabulary rather than of the corpus.
- **This is a development build, not a live instance.** The bundle is Scala.js `fastOptJS` output (7.1 MB), vendored into this repository rather than obtained from a running Direct File deployment. Declared versions upstream are `gov.irs.factgraph:fact-graph:0.1.0-SNAPSHOT` on Scala 3.3.3, exposed to Direct File's client as `@irs/js-factgraph-scala@0.0.1` — neither is a release, and both are constant across the repository's single code commit, so the commit and hash above are the only real identifiers. Running against a real instance would be the stronger comparison.

| case | graph | outcome | detail |
|---|---|---|---|
| every input supplied — complete, and outputs are distinguished from intermediates | eligibility | agrees | — |
| a declared default is used — placeholder, and it travels to dependents | eligibility | agrees | — |
| defaults apply per field, not per namespace | eligibility | agrees | — |
| an unreachable input — missing, naming the path | eligibility | agrees | — |
| missing accumulates the paths from every unresolved dependency | eligibility | differs | `eligible` — required state="missing", value=null, missing=["$.household.income","$.household.members[]"]; produced state="missing", value=null, missing=["$.household.income","$.policy.limit"] |
| an absent collection is missing, and names itself | eligibility | differs | `hasAdult` — required state="missing", value=null, missing=["$.household.members[]"]; produced state="placeholder", value=false |
| a null collection is an answer: empty, and placeholder | eligibility | agrees | — |
| an empty array is a complete answer | eligibility | agrees | — |
| a wrong-type input errors its dependents and leaves the rest intact | eligibility | agrees | — |
| an integer input is an integer to the expression | numeric | agrees | — |
| division between integers truncates | numeric | differs | `quartered` — required state="complete", value=1; produced state="complete", value=7/4 (exact fraction) |
| a number input keeps fractional semantics | numeric | differs | `scaled` — required state="complete", value=3.75; produced state="error", value=null |
| a wrong-type sub-field errors every fact reading the collection | collection | agrees | — |
| collection sub-fields read normally when well-typed | collection | agrees | — |
| an absent collection blocks every fact that reads it | collection | differs | `anyOld` — required state="missing", missing=["$.h.people[]"]; produced state="placeholder", value=false; `anyNamed` — required state="missing", missing=["$.h.people[]"]; produced state="placeholder", value=false |
| modulo | operators | cannot express | `isOdd`: FactGraph has no modulo operator, so '%' cannot be translated; `remainder`: FactGraph has no modulo operator, so '%' cannot be translated |
| conditional expression returns either branch | operators | cannot express | `band`: Unexpected character '?' at position 7 |
| conditional expression takes the false branch | operators | cannot express | `band`: Unexpected character '?' at position 7 |
| membership against a list literal | operators | cannot express | `inSet`: Unexpected character '[' at position 7 |
| membership against a list literal — absent | operators | cannot express | `inSet`: Unexpected character '[' at position 7 |

### Findings

#### missing accumulates the paths from every unresolved dependency

**Outcome:** differs  
**Detail:** `eligible` — required state="missing", value=null, missing=["$.household.income","$.household.members[]"]; produced state="missing", value=null, missing=["$.household.income","$.policy.limit"]

**Why the corpus requires this:** An output blocked on several unresolved inputs must name all of them, so a caller can ask once rather than discovering them one at a time. A path with a declared default is not missing — it resolves to the default — so it must not appear here.

#### an absent collection is missing, and names itself

**Outcome:** differs  
**Detail:** `hasAdult` — required state="missing", value=null, missing=["$.household.members[]"]; produced state="placeholder", value=false

**Why the corpus requires this:** Absent means not-yet-asked and must remain a question: a progressive interview has nothing to ask for if an uncollected list silently reads as empty.

#### division between integers truncates

**Outcome:** differs  
**Detail:** `quartered` — required state="complete", value=1; produced state="complete", value=7/4 (exact fraction)

**Why the corpus requires this:** 7 / 4 is 1, not 1.75. The declaration says integer. A fact wanting real division converts explicitly.

#### a number input keeps fractional semantics

**Outcome:** differs  
**Detail:** `scaled` — required state="complete", value=3.75; produced state="error", value=null

#### an absent collection blocks every fact that reads it

**Outcome:** differs  
**Detail:** `anyOld` — required state="missing", missing=["$.h.people[]"]; produced state="placeholder", value=false; `anyNamed` — required state="missing", missing=["$.h.people[]"]; produced state="placeholder", value=false

**Why the corpus requires this:** Same rule as the eligibility case: absent is a question, not an empty answer.

#### modulo

**Outcome:** cannot express  
**Detail:** `isOdd`: FactGraph has no modulo operator, so '%' cannot be translated; `remainder`: FactGraph has no modulo operator, so '%' cannot be translated

**Why the corpus requires this:** CEL defines % on integers. A conforming engine must support it.

#### conditional expression returns either branch

**Outcome:** cannot express  
**Detail:** `band`: Unexpected character '?' at position 7

**Why the corpus requires this:** A ternary may return any type, not only a boolean.

#### conditional expression takes the false branch

**Outcome:** cannot express  
**Detail:** `band`: Unexpected character '?' at position 7

#### membership against a list literal

**Outcome:** cannot express  
**Detail:** `inSet`: Unexpected character '[' at position 7

#### membership against a list literal — absent

**Outcome:** cannot express  
**Detail:** `inSet`: Unexpected character '[' at position 7
