# Identity shared schemas
#
# Schemas: ImmigrationInfo, Name, SocialSecurityNumber
#
# See also: schemas/common/identity.yaml (canonical JSON Schema definitions)

ImmigrationInfo:
  $ref: "../schemas/common/identity.yaml#/$defs/ImmigrationInfo"

Name:
  $ref: "../schemas/common/identity.yaml#/$defs/Name"

SocialSecurityNumber:
  $ref: "../schemas/common/identity.yaml#/$defs/SocialSecurityNumber"
