# scheduling field inventory
# Do not edit — regenerate with: node generate-field-inventory.mjs --domain=scheduling

# ── appointment ─────────────────────────────────────────────────────────────────

appointment.personId: { type: uuid, relationship: Person }
appointment.assignedToId: { type: uuid, relationship: User }
appointment.startAt: { type: datetime }
appointment.endAt: { type: datetime }
appointment.appointmentType: { type: string }
appointment.status: { type: enum, values: [scheduled, completed, canceled, no_show] }
appointment.notes: { type: string }
