# case-management field inventory
# Do not edit — regenerate with: node generate-field-inventory.mjs --domain=case-management

# ── case ────────────────────────────────────────────────────────────────────────

case.primaryApplicantId: { type: uuid, relationship: Person }
case.assignedToId: { type: uuid, relationship: User }
case.status: { type: enum, values: [active, closed] }
case.effectiveStartDate: { type: date }
case.effectiveEndDate: { type: date }
case.members[]: { type: list(CaseMember) }
case.members[].personId: { type: uuid, relationship: Person }
case.members[].relationship: { type: string }
