# users field inventory
# Generated from users-openapi.yaml
# Do not edit — regenerate with: node generate-field-inventory.mjs --domain=users

# ── user ────────────────────────────────────────────────────────────────────────

user.personId: { type: uuid, relationship: Person }
user.caseWorkerId: { type: uuid, relationship: External }
user.name: { type: string }
user.role: { type: enum, values: [applicant, case_worker, supervisor, county_admin, state_admin, partner_readonly, system] }
user.status: { type: enum, values: [active, inactive, pending, suspended] }
user.counties[]: { type: string }
user.preferences: { type: object }
