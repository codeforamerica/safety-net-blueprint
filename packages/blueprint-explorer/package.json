{
  "name": "@codeforamerica/blueprint-explorer",
  "version": "0.1.0",
  "description": "Context map, service blueprints, and documentation tooling for blueprint contract exploration",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:context-map": "node build.js --only=context-map",
    "build:data-dictionaries": "node build.js --only=data-dictionaries",
    "build:state-machine-docs": "node build.js --only=state-machine-docs",
    "build:adoption-model": "node build.js --only=adoption-model",
    "build:action-flow": "node src/sequence-diagrams/render-action-flow.js",
    "field-inventory:generate": "node src/data-dictionaries/generate-field-inventory.mjs",
    "test": "node --test tests/*.test.js tests/**/*.test.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "license": "SEE LICENSE IN LICENSE",
  "repository": {
    "type": "git",
    "url": "https://github.com/codeforamerica/safety-net-blueprint",
    "directory": "packages/blueprint-explorer"
  },
  "dependencies": {
    "@apidevtools/json-schema-ref-parser": "^11.7.2",
    "@codeforamerica/blueprint-core": "^0.1.0",
    "ajv": "^8.17.1",
    "js-yaml": "^4.1.0"
  },
  "optionalDependencies": {
    "puppeteer": "^22.0.0"
  }
}
