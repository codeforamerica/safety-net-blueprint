$schema: annotations-schema.yaml
version: "1.0"
domain: platform

facts:
  main.meetsAge:
    policies: [test-age-requirement]
    programs: [TEST]
  main.meetsIncome:
    policies: [test-income-limit]
    programs: [TEST]
  main.eligible:
    policies: [test-age-requirement, test-income-limit]
    programs: [TEST]
