---
"@codeforamerica/blueprint-cli": minor
---

`blueprint-evaluate` is a new CLI for evaluating rules from the command line. Pass a compiled graph (`*-graph.yaml`), a rules file (`*-rules.yaml`, compiled on the fly), or a rules examples file (`*-rules-examples.yaml`) to run all rulesets and examples in batch and return per-example results as JSON.
