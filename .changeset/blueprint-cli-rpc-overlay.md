---
"@codeforamerica/blueprint-cli": minor
---

`blueprint-generate-rpc-overlay` is now a published CLI binary. It reads state machine contracts and generates OpenAPI overlay files that add RPC (transition) endpoints to the base REST spec — one POST endpoint per named action in the state machine.
