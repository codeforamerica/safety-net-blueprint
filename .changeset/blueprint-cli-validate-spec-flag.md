---
"@codeforamerica/blueprint-cli": minor
---

`blueprint-validate` now accepts `--spec=<file|dir>` to validate a single file or directory without requiring the full resolved contracts directory — useful during development.
